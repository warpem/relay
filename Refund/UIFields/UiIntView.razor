@namespace Refund.UIFields
@inherits UiFieldViewBase

<RobustFluentNumberField @key="@(ValueT)" 
                   TValue="@(int)"
                   @bind-Value="@ValueT" 
                   Style="width: 100%;"
                   Class="borderless"
                   Placeholder="No value"
                   Min="@(FieldDescriptionT.Min.ToString())"
                   Max="@(FieldDescriptionT.Max.ToString())"
                   Step="@(FieldDescriptionT.StepSize.ToString())"
                   Disabled="@IsDisabled" />

@code
{
    private UiInt FieldDescriptionT => (UiInt)FieldDescription ?? null;

    private int ValueT
    {
        get => (int)Value;
        set => ValueChanged.InvokeAsync(value);
    }
}
