@using Microsoft.JSInterop
@using System.Text.RegularExpressions

<span id="slim-text-field-@(identifier)" 
min="@(Min == null ? "" : Min)" 
max="@(Max == null ? "" : Max)" 
step="@(Step == null ? "" : Step)" 
contenteditable="true" 
class="slim-text-field">@Value</span>

@code {

    

}